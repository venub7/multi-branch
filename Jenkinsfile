pipeline {
    agent any

    stages {
        stage('first step') {
            steps {
                echo 'first step'
            }
    stages {
        stage('second step') {
            steps {
                echo 'second step'
        }
    }
}
